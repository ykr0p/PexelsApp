# PexelsApp - Clean Architecture with MVVM

Это Android приложение рефакторировано с использованием принципов Clean Architecture и паттерна MVVM для лучшей организации кода и тестируемости.

## Архитектура проекта

Проект организован по принципам Clean Architecture с тремя основными слоями:

### 📱 Presentation Layer (presentation/)
UI слой, отвечающий за отображение данных пользователю
- `base/BaseViewModel.kt` - Базовый класс ViewModel с общим функционалом
- `home/HomeViewModel.kt` - Главный ViewModel для домашнего экрана
- `factory/PexelsViewModelFactory.kt` - Фабрика для создания ViewModel с зависимостями

### 🏛️ Domain Layer (domain/)
Бизнес-логика и правила приложения (не зависит от Android)
- `repository/PexelsRepository.kt` - Интерфейс репозитория
- `usecase/PexelsUseCases.kt` - Use Cases для бизнес-операций
- `model/` - Доменные модели данных

### 💾 Data Layer (data/)
Слой работы с данными (API, база данных, кэш)
- `api/` - API сервисы и модели ответа
- `local/` - Room база данных (entities, DAOs, database, converters)
- `repository/PexelsRepositoryImpl.kt` - Реализация репозитория
- `mapper/` - Мапперы для преобразования данных между слоями

## Структура файлов

```
app/src/main/java/com/example/pexelsapp/
├── MainActivity.kt                           # Главная Activity
├── domain/                                   # Доменный слой
│   ├── repository/
│   │   └── PexelsRepository.kt              # Интерфейс репозитория
│   ├── usecase/
│   │   └── PexelsUseCases.kt                # Use Cases
│   └── model/
│       ├── CuratedImage.kt                  # Доменная модель
│       └── FeaturedCollection.kt            # Доменная модель
├── data/                                     # Слой данных
│   ├── api/
│   │   ├── ApiClient.kt                     # Клиент API
│   │   ├── PexelsApiService.kt              # Сервис API
│   │   └── model/
│   │       ├── PexelsPhotoResponse.kt       # API модель
│   │       └── PexelsCollectionResponse.kt  # API модель
│   ├── local/
│   │   ├── entity/
│   │   │   ├── CuratedImageEntity.kt        # Room сущность
│   │   │   └── FeaturedCollectionEntity.kt  # Room сущность
│   │   ├── dao/
│   │   │   ├── CuratedImageDao.kt           # Room DAO
│   │   │   └── FeaturedCollectionDao.kt     # Room DAO
│   │   ├── database/
│   │   │   └── PexelsDatabase.kt            # Room база данных
│   │   └── converter/
│   │       └── Converters.kt                # Конвертеры типов
│   ├── repository/
│   │   └── PexelsRepositoryImpl.kt          # Реализация репозитория
│   └── mapper/
│       ├── CuratedImageMapper.kt            # Маппер изображений
│       └── FeaturedCollectionMapper.kt      # Маппер коллекций
└── presentation/                             # Слой представления
    ├── base/
    │   └── BaseViewModel.kt                 # Базовый ViewModel
    ├── home/
    │   └── HomeViewModel.kt                 # Главный ViewModel
    └── factory/
        └── PexelsViewModelFactory.kt        # Фабрика ViewModels
```

## Основные компоненты

### 🔄 Data Flow
1. **UI (Compose)** → **ViewModel** → **Use Case** → **Repository** → **Data Sources**
2. **Data Sources** (API/DB) → **Repository** → **Mapper** → **Domain Model** → **Use Case** → **ViewModel** → **UI State**

### 🎯 Use Cases
- `GetCuratedImagesUseCase` - Получение курированных изображений
- `GetFeaturedCollectionsUseCase` - Получение featured коллекций
- `SearchImagesUseCase` - Поиск изображений
- `RefreshDataUseCase` - Обновление данных
- `ClearCacheUseCase` - Очистка кэша

### 🗄️ База данных
- **Room** для локального хранения данных
- Автоматическое кэширование API ответов
- Fallback данные при отсутствии интернета

### 🌐 Сеть
- **Retrofit** для HTTP запросов к Pexels API
- GSON для сериализации/десериализации
- Обработка ошибок сети

## Преимущества архитектуры

### ✅ Чистое разделение ответственности
- **Presentation** - только UI логика
- **Domain** - бизнес-правила
- **Data** - работа с источниками данных

### ✅ Тестируемость
- Каждый слой можно тестировать отдельно
- Use Cases не зависят от Android
- Моки легко подставляются в тестах

### ✅ Масштабируемость
- Легко добавлять новые функции
- Простое расширение источников данных
- Модульная архитектура

### ✅ Поддержка
- Четкая структура проекта
- Легко найти нужный код
- Понятные зависимости между компонентами

## Зависимости

Проект использует современные Android библиотеки:
- **Jetpack Compose** - UI
- **Room** - локальная база данных
- **Retrofit** - сетевые запросы
- **Coroutines** - асинхронность
- **ViewModel & StateFlow** - MVVM
- **Hilt** - dependency injection (готов для использования)

## Запуск проекта

1. Откройте проект в Android Studio
2. Синхронизируйте Gradle зависимости
3. Запустите приложение на эмуляторе или устройстве

## Будущие улучшения

- [ ] Добавить Hilt для автоматического dependency injection
- [ ] Реализовать pagination с Paging 3
- [ ] Добавить unit тесты для Use Cases
- [ ] Добавить инструментальные тесты для UI
- [ ] Реализовать offline-first подход с синхронизацией